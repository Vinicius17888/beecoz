import { theme } from "../theme/theme";

// compat: alguns arquivos esperam THEME vindo de @lib/theme
export const THEME = theme;

// exporta também com o nome padrão que usamos hoje
export { theme };
